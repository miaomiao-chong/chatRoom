import PropTypes from 'prop-types'
import React, { memo } from 'react'

const BossInfo = memo((props) => {
  return (
    <div>BossInfo</div>
  )
})

// BossInfo.propTypes = {}

export default BossInfo